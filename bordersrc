#!/bin/bash

# JankyBorders Configuration
# A lightweight border utility for macOS windows

# Basic border settings
borders active_color=0xffff79c6 \
        inactive_color=0x00000000 \
        width=3.0 \
        hidpi=on \
        style=square &

# Color scheme (Pink active, transparent inactive)
# Active: Pink (matching SketchyBar theme)
# Inactive: Transparent (completely hidden)

# Border styles available:
# - square: Sharp corners (current)
# - round: Rounded corners

# Width: Border thickness in pixels
# HiDPI: Enable high DPI support for retina displays