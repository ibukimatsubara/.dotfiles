# Copy to ~/.config/svu-power/config on the machine that sends wake packets.
# Keep machine-specific values in the local copy, not in Git.
SVU_WAKE_MAC=""
SVU_WAKE_BROADCAST="192.168.0.255"

# SSH aliases from ~/.ssh/config.
SVU_SSH_HOST="svu"
SVU_COORDINATOR_HOST="svm"
SVU_WAKE_WAIT_SECONDS="90"
