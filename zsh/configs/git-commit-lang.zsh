# Git commit message language configuration
# デフォルトのコミットメッセージ言語を設定
# "ja" for Japanese (default), "en" for English

export GIT_COMMIT_LANG="ja"

# プロジェクトごとに言語を切り替えたい場合は
# .envrcや.zshrc.localに以下を追加:
# export GIT_COMMIT_LANG="en"