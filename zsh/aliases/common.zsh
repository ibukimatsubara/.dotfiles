# Common aliases
alias c='clear'
alias ll='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias lal='ls -la'

# Claude MCP aliases
alias sere='claude mcp add serena -- <serena-mcp-server> --context ide-assistant --project $(pwd)'

